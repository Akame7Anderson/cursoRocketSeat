// function expression
// function anonymous

// parâmetros - parameters
const sum = function(number1, number2)
{
    let total = number1 + number2
    return total
}

// sum(1, 5) // arguments - argumentos 
let number1 = 15
let number2 = 10

console.log(`O total do número ${number1} + o número ${number2} é ${number1 + number2}`)